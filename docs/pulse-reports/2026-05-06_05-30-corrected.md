# ai-pipeline-template — Product Pulse (CORRECTED)

**Window:** 2026-05-05 03:14 UTC → 2026-05-06 03:14 UTC (24h)
**Generated:** 2026-05-06 05:30 local (UTC+03:00) — corrected after user feedback that the original pulse measured pipeline self-maintenance and called it product convergence

## Headlines (corrected)

- **Convergence engine produced 1 merged product PR (wgmesh#564) and 1 still-open impl PR (wgmesh#563)** in 24h. Spec-writing is functioning (2 specs landed); impl→merge is 50% (1 of 2 impls merged in window).
- **2 issues stuck in `copilot-triaging` for >24h**: wgmesh#515 (23 days, no movement), wgmesh#540 (8 days, reopened today). A third (wgmesh#561) was opened today and is currently in `copilot-triaging` — included in the count below for completeness but not "stuck" by the >24h definition.
- **All 12 ai-pipeline-template "bot PRs"** in this window are pipeline self-maintenance (`heal: pipeline health check`). They don't measure product convergence.

## Product flow (wgmesh — where the seeded product lives)

| Stage | Count | PRs (all in wgmesh) |
|---|---:|---|
| `spec_pr_opened` | 2 | wgmesh#557 (for #556), wgmesh#562 (for #561) |
| `impl_pr_opened` | 2 | wgmesh#563 (for #561), wgmesh#564 (for #556) |
| `impl_pr_merged` | 1 | wgmesh#564 |
| `impl_pr_stuck_open` | 1 | wgmesh#563 (still open since 2026-05-05 11:49) |
| Issues currently in `copilot-triaging` | 3 | wgmesh#515 (23d), wgmesh#540 (8d), wgmesh#561 (today) |
| Of those, stuck >24h | 2 | wgmesh#515, wgmesh#540 |

wgmesh#564 was the one that exposed the GitHub `Closes #N` keyword bypass — gate workflows ran but the issue was already auto-closed. Tracked in wgmesh PR #567 (reopen-on-bypass guard).

## Meta flow (ai-pipeline-template — the framework, not the product)

| Source | Count | Notes |
|---|---:|---|
| `bot_pr_merged` (heal:) | 12 | 100% clean (zero escalations) — confirms ai-pipeline-template PR #706 fix held |
| `loop:` daily-assessment PRs | 3 | All by `nycterent` (PAT-leak pattern still in ai-pipeline-template; PRs #675/#699/#703/#706 only fixed pipeline-health and bot-pr-review-merge) |
| `paid_customers` | 0 | chimney HTML scrape (no Polar integration) |

## Process flow (cross-repo — meta + product process gates)

| Source | Count | Notes |
|---|---:|---|
| Process-gate PRs in wgmesh | 5 | wgmesh#558, #559, #560, #565, #566 — all close-loop hardening |

## System performance

Zero workflow failures across 11 active workflows. ai-pipeline-template + wgmesh combined.

## Followups (corrected priority)

1. **wgmesh#515 stalled 23 days.** Was opened 2026-04-12, hasn't moved past `copilot-triaging`. Root cause: spec PR never created or never approved-for-build. Investigate why Copilot didn't pick it up.
2. **wgmesh#563 (impl for wgmesh#561) still open** since 2026-05-05 11:49 (~16h). What's blocking the merge — CI failures, review comments, or just queue lag?
3. **GitHub `Closes #N` keyword bypasses test gate** (discovered today via wgmesh#556 incident). The bypass was tracked in wgmesh PR #567 (reopen-on-bypass guard). Goose template change to `Implements #N` would prevent the flap visibility entirely.
4. **Pulse config measured wrong target — fixed in this PR (#736).** `pulse_primary_event = bot_pr_merged` lumped maintenance with product. STRATEGY.md now warns "Convergence is measured in the SEEDED PRODUCT repo, not in `ai-pipeline-template`" and redefines `Goose autonomous-ship rate` to filter on seed-repo + `impl:` titles. The `.compound-engineering/config.local.yaml` change to `pulse_primary_event = product_pr_merged` is local-only (gitignored) — documented in this PR's description, not in the commit diff.
5. **Polar integration** still pending. `cycle_time_to_revenue` and `paid_customers` stuck at null/0.

## Original pulse error (2026-05-06 05:30)

The headline "12 bot PRs merged, 100% autonomous-ship rate" was technically true but materially false: those 12 PRs were ALL `heal: pipeline health check` from `pipeline-health.yml`'s 30-min cron. They tell us the maintenance cron is healthy, not that products are shipping. STRATEGY.md's `Goose autonomous-ship rate` was conceived as "% of seeded specs that merge → deploy with zero human intervention". The pulse measured something else.
